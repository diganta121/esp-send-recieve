# esp-now-send-recieve

reciever acts as the master that all the transmitters connect to through wifi based esp-now protocol 


each transmitter has an unique id and ip address that the transmitter uses to keep track of each of them